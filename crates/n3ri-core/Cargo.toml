[package]
name = "n3ri-core"
version = "0.1.0"
edition = "2021"
description = "Core engine for n3ri_os pseudo-operating system"

[dependencies]
bevy = { workspace = true, features = [
    "bevy_asset",
    "bevy_log",
    "bevy_state",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
